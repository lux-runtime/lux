# Default Ignore
# Build Artifacts
target/
/build/
**/*.dll
**/*.exe
**/*.pdb
**/*.obj
**/*.lib

# Logs and Debug Output
*.log
*.txt
debug_*.luau
errlog.txt
stderr.txt
debug.txt

# Editor / IDE
.vscode/
.idea/
*.iml
*.swp

# System
Thumbs.db
.DS_Store

# Temporary
tmp/
script.luau
convert_icon.luau

# Explicitly Keep (implicit via not ignoring)
# We want: crates/, bin/, assets/, lux_packages/ (if source), Cargo.toml, Cargo.lock, LICENSE, README.md, build.bat, test.bat, stylua.toml, .gitattributes, tests/ (Wait, user said "não arquivos de teste", so ignoring tests/)

# User specific exclusions
lux_packages/
